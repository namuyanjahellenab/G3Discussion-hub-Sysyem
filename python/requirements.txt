# Runtime dependencies - versions confirmed against what's actually
# installed and imported by app.py/catalog.py/config.py/db.py/pdf.py/
# share.py/spam.py in this environment.
Flask==3.0.3
Werkzeug==3.0.1
scikit-learn==1.6.0
PyMySQL==1.2.0
python-dotenv==1.0.1
Pillow==12.3.0

# pdf.py's PDF export - after installing this, also run once:
#   playwright install chromium
# (downloads the actual browser binary Playwright drives; not covered by pip alone)
playwright==1.61.0

# Test-only dependency (test_app.py / `pytest` in the README) - not
# imported by the running gateway itself.
pytest==7.4.0
